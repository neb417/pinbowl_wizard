<div id="<%= dom_id machine %>">
  <p>
    <strong>Name:</strong>
    <%= machine.name %>
    <%= link_to 'Edit', edit_machine_path(machine), data: { turbo_stream: true } %>
    <%= link_to 'Delete', machine_path(machine), data: { turbo_method: :delete } %>
  </p>

</div>
